{
  "name": "error-buffer-boolean-distance",
  "tool": "buffer",
  "description": "A distance must be a number or a numeric string. The two languages coerce other JSON types differently (Number(false) is 0, Python's `false or 0` is 0 but `float([5])` raises), so both engines reject the type outright rather than each picking a coercion.",
  "input": {
    "type": "FeatureCollection",
    "features": [
      {
        "type": "Feature",
        "properties": {},
        "geometry": {
          "type": "Point",
          "coordinates": [-78.0, 35.0]
        }
      }
    ]
  },
  "parameters": {
    "distance": false,
    "units": "kilometers"
  },
  "expect": {
    "error": true
  }
}
