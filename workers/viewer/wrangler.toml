name = "geolibre-viewer"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# viewer.geolibre.app is the original hostname and remains as a legacy alias.
# web.geolibre.app is hosted directly by GitHub Pages; see web-deploy.yml.
[[routes]]
pattern = "viewer.geolibre.app"
custom_domain = true
